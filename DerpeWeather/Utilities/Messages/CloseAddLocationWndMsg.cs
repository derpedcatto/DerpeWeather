namespace DerpeWeather.Utilities.Messages
{
    /// <summary>
    /// Empty message that gets sent from <see cref="AddLocationVM"/>
    /// to <see cref="AddLocationWindow"/> and signals that it needs to close.
    /// </summary>
    public class CloseAddLocationWndMsg
    {
    }
}
